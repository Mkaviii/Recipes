## Recipes plan Backend Application

# Description

The Recipes Planner Backend Application is a RESTFUL API built with Node.js and Express.It allows users to manage their meal plains,including creating,reading,updating and deleting meal entries.The application uses Mongodb as its database to store meal data.

## steps

1. Repo setup and NPM initialization
   -Empty folder creation
   -npm init command
   -Empty respository creation on GitHub
2.  Git Initialization
   - readme.md file creation
   - git initialization with git init command
   -connect local repo with remote  repo 

   git init
   git remote add orgin<remote-repo-URL>
   git add .
   git commit -m 'first change'
   git branch -M main
   git push -u orgin main